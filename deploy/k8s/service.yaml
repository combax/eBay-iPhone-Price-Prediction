apiVersion: v1
kind: Service
metadata:
  name: ebay-price-go
spec:
  selector:
    app: ebay-price-go
  ports:
    - port: 80
      targetPort: 8080
  # ClusterIP by default; switch to LoadBalancer (or add an Ingress) to expose it
